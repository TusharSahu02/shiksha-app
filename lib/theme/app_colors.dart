import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary - Deep Royal Purple
  static const Color primary = Color(0xFF4B2D8E);

  // Primary Dark - Navy Purple
  static const Color primaryDark = Color(0xFF2D2154);

  // Secondary - Amber Orange
  static const Color secondary = Color(0xFFF0A030);

  // Accent - Gold
  static const Color accent = Color(0xFFD4A017);

  // Background
  static const Color background = Color(0xFFFFFFFF);

  // Surface
  static const Color surface = Color(0xFFF8F6FC);

  // Text colors
  static const Color textPrimary = Color(0xFF2D2154);
  static const Color textSecondary = Color(0xFF6B6B6B);
  static const Color textOnPrimary = Color(0xFFFFFFFF);
  static const Color textOnSecondary = Color(0xFF2D2154);
}
